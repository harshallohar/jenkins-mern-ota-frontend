version: '3.8'

services:
  frontend:
    build:
      context: .
    ports:
      - "80:80"
    container_name: my-frontend-v2
    restart: always
    environment:
      - NODE_ENV=production  this is m my docker-composefile 
